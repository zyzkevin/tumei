<view class="container">

      <view class="grid-container">
    <block wx:for="{{showJsonList}}" wx:key = "item">
      <view class="movie-container">

    </view>
      <navigator url="../postDetails/postDetails?id={{item.id}}" hover-class="navigator-hover">  
          <image class="postImg" src="../../images/{{item.id}}.jpg"  width="355" height="150" mode="aspectFit"/>  
        <text class="postTitle">{{item.title}}</text>
      </navigator>  
    </block>  
  </view>
</view>